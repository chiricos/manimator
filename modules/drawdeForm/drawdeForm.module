<?php
/**
 * Theme hook
 */
function drawdeForm_theme($existing, $type, $theme, $path) {

    return array('drawdeForm' =>
        array(
            'variables' => array(
                'titulo' => 'Título por defecto',
                'descripcion' => 'descripción por defecto'
            ),
            'template' => 'mi-bloque'
        ));
}